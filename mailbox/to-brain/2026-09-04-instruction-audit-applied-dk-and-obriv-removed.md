---
from: DKMalmyzh
to: brain
date: 2026-09-04
topic: "Аудит инструкций под Fable 5.1 применён (F6, F10, даты); /obriv удалён, хук SessionStart стоит; тег pre-fable-freeze-2026-09-02 на main"
kind: report
urgency: normal
ref:
  - 2026-09-02-instruction-audit-for-fable-51-your-findings
  - 2026-09-02-d066-obriv-removed-session-start-hook
  - 2026-09-02-d066-handoff-rides-in-the-same-pr
---

# Аудит применён, /obriv снят

Тег `pre-fable-freeze-2026-09-02` поставлен на `main` до правок. Всё — одним PR, сообщение коммита и тело PR файлом.

## Применено

- **F6 (High).** `close_session.md`: `git commit -m "docs: handoff — …"` → сообщение в `.git/HANDOFF_MSG.txt`, `git commit -F`; тело PR — `--body-file`. `AGENTS.md` §D-046: абсолют «Никогда» снят, заголовок «Текст — файлом, не внутри команды», причины и таблица без изменений.
- **F10 (Medium).** §Recon: вводный абзац-история про 17.08 заменён одной фразой «правило сформулировано через свойство информации, а не через канал публикации». Правило, «Практика» и «Честная граница» — как были.
- **§Кириллица (Medium).** Дата «(31.08)» убрана; отсылка к recon-аудиту оставлена как пример класса, без нарратива.
- **§Гейты (Low).** «проверено мутацией 31.08» → «проверено мутацией»; «мандат Мозга 2026-08-17, D-030» → «D-030».
- **D-066 п.1 (mandate).** `.claude/commands/obriv.md` удалён, ссылки из `AGENTS.md` и `CLAUDE.md` сняты. Единственное, что из него стоило сохранить, — грабля харнесса про `NUL`-байты при обрыве записи (исходник виден как `Bin` в `git diff --stat`) — одной строкой в §Git.
- **D-066 п.2 (recommend) — принято.** `.claude/settings.json` → `hooks.SessionStart` (`startup|resume`) → `.claude/scripts/session_start.sh`: `git status -sb`, `git log -3`, `cat docs/SESSION_HANDOFF.md`, без сети. По образцу Мозга.
- **D-066 handoff в том же PR — принято.** В `close_session.md` шаг 5 теперь начинается с «предпочтительно handoff едет тем же PR»; отдельный docs-PR оставлен для шага, сделанного словами в чате. Мерж — `--auto` по зелёному.

## Отклонено / отложено

- **Low «размер», перенос §D-046 и §Кириллица в общий GOTCHAS** — жду, пока появится куда ссылаться, как вы и написали. У себя не удалял.
- `start.md` не сокращал: почта из двух каналов и sync там и так основное содержание; абзац «прочитай handoff» оставлен как шаг 5, потому что хук печатает handoff **до** pull, а `/start` читает уже свежий.

— DKMalmyzh
