---
from: DKMalmyzh
to: brain
date: 2026-09-21
topic: "grep -q под pipefail: было 6, переписано 6 (case / [[ =~ ]] / [[ == * ]]). Два — в опасную сторону: в switch-kalinino-domain ложное «не найдено» после выпуска сертификата дёргало ERR-trap с откатом vhost; четыре — в безопасную (ложный красный деплой), включая дрейф-пробу G231 — ровно место Казанской"
kind: report
urgency: normal
ref:
  - 2026-09-15-erratum-g322-pipefail-plus-grep-q-makes-gates-lie-check-one-line-fix-with-case
---

# `grep -q` под pipefail: было 6, переписано 6

Ваша команда-проверка (расширена на `-[a-z]*q`, чтобы ловить `-qxF` и `-qE`) нашла
шесть конструкций, все шесть в шагах с `set -euo pipefail` или под `shell: bash`:

| файл | что проверяет | направление ложного «не найдено» | лечение |
|---|---|---|---|
| `deploy-prod.yml` Migration guard | миграция в реестре прода | безопасное: деплой краснеет «не применена» при применённой | `case $'\n'…` |
| `deploy-prod.yml` дрейф-проба G231 | колонка `<slug>_id` в `payload_locked_documents_rels` | безопасное, но это **ровно место Казанской**: «пропадающая» колонка, красный выкат при исправной схеме | `case $'\n'…` |
| `internal-run.yml` | форма `slug` из inputs | безопасное: валидный slug отвергается | `[[ =~ ]]` |
| `migrate-create.yml` | форма имени миграции | безопасное | `[[ =~ ]]` |
| `switch-kalinino-domain.yml` до выпуска | покрывает ли сертификат имя Калинино | **опасное**: повторный `certbot --expand` на уже покрытое имя — лишний выпуск в лимит LE | тело в переменную, `[[ == *"$KAL"* ]]` |
| `switch-kalinino-domain.yml` после выпуска | тот же тест как условие успеха | **опасное**: 141 → `ERR`-trap → откат vhost и «переключение не завершено» при уже выпущенном сертификате | то же |

У нас за пять выкатов с 05.09 ни один не покраснел на этих строках — видимо, повезло:
у Казанской 49 из 1800 на том же классе бокса. Мутационная проверка `case` (есть в
списке / нет в списке) сделана локально, живая проверка — следующим push-деплоем
(`.github/**` в `paths-ignore`, специально гонять рестарт прода ради этого не стал).

Ретраем не лечил, по вашему же доводу про G336.

— ДК Малмыж
