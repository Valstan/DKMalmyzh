# ДК Малмыж — Session Handoff

> Sticky-note для непрерывности сессий. Обновляется тем же PR, что и шаг работы (D-066); `/close_session` — страховка. История — `git log -- docs/SESSION_HANDOFF.md`.

**Status:** LIVE — импорт из ВК включён; Калинино поглощено (D-074), третье имя портала отдаёт 301
**Updated:** 2026-09-05, ночь (перенос Калинино выполнен: 48 записей, 225 медиа, 44/44 редиректов; письмо-приёмка Мозгу отправлена)
**Branch:** main

## Текущая нитка

Портал культуры Малмыжского района — https://культура.вмалмыже.рф/ (punycode `xn--80atdujec4e.xn--80adkdyec4j.xn--p1ai`); прежний `домкультуры.вмалмыже.рф` отдаёт 301, `сдк-калинино.вмалмыже.рф` (с 05.09) — 301 на `/dk/kalinino` и путь-в-путь для `/news/<slug>`. Одна линия сертификата LE на три имени до 2026-12-03.

**Ключ шлюза SARAFAN на проде.** Забран 04.09 паспортом КАРМАНа (OIDC → `/api/secrets/session` → `GET ?key=`), шаг живёт в `deploy-prod.yml` и повторяется каждым деплоем идемпотентно (env без изменений — рестарта нет). Проба на шлюзе с бокса: свой ключ читает стену, чужой — 401. `INTERNAL_OPS_SECRET` сгенерирован на боксе тем же шагом. Таймер `dkmalmyzh-vk-sync.timer` включён (раз в полчаса).

⚠️ Инфра-детали прода (адрес хоста, порт, состав бокса, адреса шлюза и vault) в репозитории **не хранятся** — `AGENTS.md` §Recon-поверхность. Адреса лежат в `secrets.VAULT_URL` / `secrets.GATEWAY_URL`.

## Выполнено 2026-09-05 — поглощение Калинино (D-074, #61–#64)

Мозг принял план с тремя условиями (письмо 04.09); все три закрыты кодом и гейтом, перенос выполнен на проде, приёмка отправлена письмом `2026-09-05-d074-kalinino-lives-in-the-portal…`.

- **Модель:** `posts.videos` (array), `posts.sourceUrl`, `institutions.theme` (select, пока `kalinino`). Миграция `20260904_195445_kalinino` сгенерирована на раннере новым воркфлоу `migrate-create.yml` (артефакт .ts/.sql/.json; `workflow_dispatch` виден только с `main` — файл ехал отдельным PR #61). Применена `apply-migration.yml`, деплой dispatch, G231 «5 = 5».
- **Операция `/internal/kalinino-transfer`** (`?dir=` абсолютный путь строгой формы, `?dry=1`): читает `posts.json`/`categories.json`/`media/` с бокса, идемпотентна по `vkUid` (= их `vkPostId`), slug и публикация владельца сохраняются, коллизии с чужими записями — 409 и стоп, повторы внутри выгрузки — новейшая оставляет адрес (как отдавал их сайт), старшая получает хвост из vkPostId. Публикует карточку-получатель. `internal-run.yml` находит каталог по маске, распаковывает `media.tar.gz` под nice, сверяет published выгрузки с публичным API живого сайта Калинино (46 = 46).
- **Итог на проде:** создано 28, обновлено 20 (уже были от нашего импорта со стены; своё медиа оставлено — 70 файлов), ошибок 0, загружено 155 файлов, видео 9 у 6 записей, sourceUrl 48/48, без текста 12 (афиши-картинки). Повторный прогон: создано 0, загружено 0.
- **Тема раздела** по пути: `SectionTheme` на `/dk/<slug>` и записях учреждения с `theme` — лента с фразой, эмблема, гирлянда, палитра вышивки, шрифт Kurale; ассеты в `public/brand/`. Шапка и подвал портала общие.
- **Домен:** `switch-kalinino-domain.yml` (`check` / `switch` / `restore`): снял их vhost с записью для возврата, расширил сертификат третьим именем, поставил `deploy/nginx-kalinino-redirect.conf` отдельным файлом (в основной vhost не вписан намеренно), приёмка `deploy/kalinino-redirect-check.py` — 44 опубликованных slug, 0 расхождений. Их юнит/таймер/ключ не трогались.
- **Проверено снаружи:** `/dk/kalinino` 200 с темой и лентой, `/news/уважаемые-друзья` 200 с темой, старое имя `/` → 301 `/dk/kalinino`.

### Грабли этой сессии

1. **Сухой прогон окупился дважды.** Записи без текста (12) помечались «не lexical — будет пропущена»; карточка-получатель была черновиком, и раздел отдавал 404 — 46 редиректов вели бы на 404. Оба класса — #279 «отчёт зелёный, контента нет».
2. **`systemctl reload nginx` асинхронный:** проба сразу после reload прошла через старые воркеры с прежним конфигом (301 вместо 200), переключение откатилось само. Проба ждёт до 15 с.
3. **`nginx -t` на общем боксе печатает имена соседних vhost** в предупреждениях `protocol options redefined` — первый прогон переключения вынес их в публичный лог (D-038, не отозвать). Фильтр с двух сторон: grep на боксе, sed в `redact`.
4. **`workflow_dispatch` GitHub ищет только на default-ветке:** новый воркфлоу с ветки не дёрнуть, пока файла нет в `main`.
5. Python `open(..., 'w')` на Windows пишет CRLF — git нормализует при коммите, но рабочая копия остаётся с CRLF (предупреждения при `git add`, безвредно).

## Выполнено 2026-09-04 (#43–#45)

- **D-066 (#43).** `/obriv` удалён, хук `SessionStart` (`.claude/scripts/session_start.sh`) печатает git status/log и handoff при открытии сессии. Аудит инструкций под Fable 5.1 применён: `close_session` коммитит через `git commit -F`, §D-046 без абсолюта «Никогда», §Recon без абзаца-истории, даты из §Гейты/§Кириллица убраны. Тег `pre-fable-freeze-2026-09-02` на `main`.
- **Паспорт КАРМАН (#44).** Первый же прогон — 201: личность уже была заведена. Ключи `GATEWAY_KEY_DKMALMYZH` и `ECOSYSTEM_KEY` в env бокса. Дрейф-проба G231 встроена в Migration guard: slug каждой коллекции обязан иметь колонку в `payload_locked_documents_rels` (сработает на первом push-деплое; dispatch guard пропускает).
- **Каталог на проде:** `seed-institutions` — создано 31, 27 со ссылками, 33 источника, 10 карточек в `needsReview` (список в логе прогона). Все **черновиками** — публикует владелец.
- **Первый импорт из ВК:** см. строку ниже в «Что показал первый импорт».
- **Праздники района (#45, D-075).** `/prazdniki` + блок на главной: карточки-ссылки на Сабантуй (заглушка до письма) и Казанскую (как прислана). Данные — `web/src/lib/festivals.ts`. Пункт меню в шапке — глобал `SiteHeader` в админке, кодом не трогали.
- **Письма Мозгу:** ответ по аудиту и D-066; план поглощения Калинино с тремя скептиками (D-074) + приёмка выгрузки + строка по G231 + D-075.

## Что показал первый импорт (04.09, проба `probe-prod.yml`)

- **632 записи черновиками**, 2457 медиа, **33/33 источника разрешены** в owner_id. По учреждениям: у ДК с двумя источниками ~40, у остальных 20 (лимит `VK_SYNC_COUNT`), Гоньба 19, Каксинвай‑2 пуст по факту (оговорка Сарафана).
- **Ручной прогон шёл дольше 20 минут** — curl в `internal-run.yml` оборвался, результат в логе не появился, на боксе прогон дошёл до конца. Таймер в это время запустил **второй прогон поверх первого**: дубли упёрлись в уникальность `vkUid` и имени файла в Media («The following field is invalid: vkUid/filename», «с ошибкой 15»). Это не потеря данных — записи создал первый прогон. Починено (#45): замок в маршруте (409 «прогон уже идёт»), `max-time` 3900 в воркфлоу и 2700 в юните.
- Смотреть состояние прода — `probe-prod.yml` (#46): юниты, журнал без recon-полей, счётчики в БД.

## Аудит вечера 04.09 (#51, #52) — четыре зелёные проверки, не проверявшие ничего

Многоагентный аудит собственных изменений дня. Каждая находка перечитана по коду, все починены и уехали на прод.

- **Дрейф-проба G231 стояла внутри `Migration guard` с `if: event_name != workflow_dispatch`** — то есть выключалась ровно на том пути, которым миграции и приезжают на прод. Вынесена отдельным шагом без условия; список коллекций берётся из массива `collections` в `payload.config.ts`, а не globом по каталогу; неразобранный slug роняет шаг. Первый живой прогон: «колонок 5 = коллекций 5, чисто».
- **Замок импорта имел гонку:** проверка до `await getPayload`, установка после — два запроса проходили оба, то есть вчерашнее лекарство от аварии её не предотвращало. Замок вынесен в `web/src/lib/internal/lock.ts`, берётся синхронно, общий с `seed-institutions`. Мутация: старая схема пускает 3 из 3 параллельных вызовов, новая — 1.
- **`alternates: { canonical: '/' }` в корневом layout** проставлял канониклом главную КАЖДОЙ странице при живом sitemap — разделы выпадали бы из индекса. Канонический адрес задаётся постранично через `canonicalOf`; проверено на проде.
- **`inputs.slug` интерполировался в тело `run:`** — кавычка в значении выполняла бы произвольную команду в шаге с приватным деплой-ключом. Теперь через `env` и с валидацией.
- Попутно: `trap` без `exit` на INT/TERM мог записать в прод-env пустые ключи; файлы с ключом оставались в `/tmp` бокса; тело ответа шлюза и длина с sha256-префиксом ключей печатались в публичный лог; `curl --fail` в юните глушил тело (409 и 503 выглядели одинаково) — запуск идёт через `deploy/dkmalmyzh-vk-sync-run.sh`; `Persistent=true` снят.

⚠️ **Сам аудит с первого раза не состоялся:** девять агентов подряд упали на сетевых и авторизационных ошибках API, вернув пустой результат. Пустой результат такого прогона означает «не проверено», а не «чисто».

## Второй аудит: данные (#56–#59)

Измерения по живым данным дали ещё восемь находок, две с потерей.

- **Импорт стирал источники ВК из карточки.** `runVkSync` перезаписывал `institutions.vkSources` массивом только тех источников, которые удалось разрезолвить в этот прогон. Флаг «что-то изменилось» общий на учреждение, поэтому ссылка, поймавшая 429 или переименование, исчезала, как только сосед по массиву обновил свой `ownerId`. У головного РЦКД пара «короткое имя + числовой адрес» — числовой всегда проходит, короткое имя ходит в шлюз. Теперь обходится исходный список, меняется только кэш.
- **123 записи под 49 одинаковыми адресами.** Slug считался из заголовка, а заголовки импорта повторяются по построению. Поле не `unique`, страница отдаёт первый попавшийся документ — при публикации порядка 74 материалов были бы недоступны при живых ссылках из ленты. Slug теперь несёт `vkUid`; существующие записи переименованы операцией `/internal/reslug-vk`. **Проверено пробой: дублей 0 из 636.**
- Дедлайны у обращений наружу (шлюз 30 с, фото 20 с), вырезание серверных путей из сообщений об ошибках, черновики без ревалидации, правила редактирования в пробе по форме (IPv4, баннер Next, проза psql).

### Три грабли, каждая сама выглядела как успех

1. **Проба не могла покраснеть.** `ssh … | redact` под дефолтной оболочкой `bash -e` без `pipefail`: кодом шага становился код `sed`. Приёмка выгрузки Калинино держалась на проверке, которая физически не могла провалиться. Лечится `shell: bash`.
2. **Правка ушла в версию, а не в запись.** `payload.update` с `draft: true` пишет в `_posts_v`, а `posts.slug` остаётся прежним. Операция отрапортовала «переименовано 636, с ошибкой 0», проба показала те же 49 дублей. Симметрично: `payload.find` с `draft: true` читает версии — второй прогон ответил «без изменений 636», глядя на уже исправленные версии при старой основной записи. Теперь операция и пишет, и читает основную запись, а после каждого обновления перечитывает документ и сверяет адрес.
3. **`ssh` не передаёт argv.** Он склеивает аргументы в строку, удалённый шелл разбирает её заново: пустой аргумент исчезает, следующие сдвигаются. Запуск с пустым `slug` и `dry_run=true` отдал «true» в slug — «сухой» прогон оказался боевым и честно написал `dryRun: false`. Значения передаются именованными переменными.

## Выгрузка Калинино сверена (04.09, `verify-handover.yml` #47)

Каталог `handover-2026-09-04` на боксе, 599 МБ: `posts.json` 48, `categories.json` 6, `media.json` 270; манифест медиа — 945 файлов, из них 131 `db-unlinked`, к переносу 814; sha256 архива сошёлся с приложенным.

**Расхождение по `manual-edits.json` снято фактом.** Файл не пустой (14 850 байт), это отчёт по 17 записям с флагами `changedSinceImport`. Счётчики: `title` 0 из 17, `content` 0, `date` 0, `slug` 0; `status` 17, `cover` 15, `categoryId` 11. То есть ручных правок текста действительно нет, а повторный импорт потерял бы публикацию, обложки и рубрики — выбор «переносим из выгрузки» подтверждён числом, а не доверием.

## Следующий шаг

1. **Владельцу:** пройти 31 карточку в админке (`/admin` → Дома культуры), опубликовать готовые (Калинино уже опубликовано переносом); 10 карточек требуют решения. Проверить черновики новостей после импорта; когда результат устроит — `VK_SYNC_PUBLISH=1` в env бокса. Посмотреть раздел `/dk/kalinino` глазами: тема, видео на записях.
2. **Ждать ответа Мозга** на приёмку D-074 (выключение юнита Калинино и отзыв их ключа — не наша сторона).
3. **Сабантуй пришлёт карточку** — заменить заглушку в `festivals.ts`.
4. Рубрикатор Калинино (`lib/vk-sync.ts` в их репо, 27/29) — кандидат в наш импорт для всех ДК, отдельным PR; замер прислать Мозгу.
5. Выгрузка на боксе (`handover-2026-09-04`, 599 МБ + распакованное `media/`) — убрать после «принято» от Мозга.

## Грабли и находки этой сессии

- **`gh pr merge --auto` в репо выключен** (`enablePullRequestAutoMerge`) — мерж руками по зелёному.
- **`.github/**` в `paths-ignore` деплоя:** правка workflow не выкатывается сама, нужен `gh workflow run deploy-prod.yml --ref main`; при dispatch Migration guard пропускается.
- **Сеть до GitHub мигает** (TLS handshake timeout) — команды `gh` оборачивать в повтор. У соседей то же самое, их обход — SOCKS-туннель через бокс (`ssh -N -D 1080`, `https_proxy=socks5h://…`).
- **Адрес соседнего сайта берём у самого соседа, а не из письма.** В письме Мозга по D-075 назван прежний домен Сабантуя; их деплой проверяет, что он отдаёт 301 на `сабантуй.вмалмыже.рф`. Ссылка через редирект живёт до непродления регистрации — прецедент дкмалмыж.рф.
- **Многоагентный аудит 04.09 с первого раза не состоялся:** девять агентов подряд упали на сетевых и авторизационных ошибках API. Пустой результат такого прогона означает «не проверено», а не «чисто» — перезапускать, а не засчитывать. Со второго раза прошёл и дал четыре блокера.
- **Канал этой машины фильтрует избирательно.** `1.1.1.1` и GitHub API отвечают за доли секунды, наш прод — TLS-рукопожатие 12,5 с при мгновенном TTFB после него, `example.com` и сайты праздников не соединяются вовсе (пинг проходит, TCP нет). Медленность прода, замеренная отсюда, — свойство канала, а не сервера: проверять изнутри бокса. Обход у соседей — SOCKS-туннель через бокс.
- Прежние грабли живы: бокс делят несколько сайтов (маркер «Малмыж»); TLS терминирует nginx; кириллица в `grep -i` не матчится; heredoc в Bash-инструменте съедает `\`+перенос.

## Служебные операции на проде

`/internal/seed-institutions`, `/internal/vk-sync` и `/internal/reslug-vk` (разовое приведение адресов записей к уникальным, `?dry=1`) исполняет само приложение; запуск вручную — `internal-run.yml`. Все три берут **общий замок**: параллельно идёт одна пишущая операция, вторая получает 409. Подробности — `AGENTS.md` §Служебные операции. Импорт — только через шлюз SARAFAN (§ВКонтакте): только `owner_id`, пауза 2,1 с, `429` штатно; всё приезжает **черновиками** и новостью, афишу ставит редактор.

## Открытые вопросы владельцу

- **10 карточек каталога требуют решения** (лог прогона `seed-institutions` 04.09): три учреждения ведут личную страницу вместо сообщества, у четырёх сообщества нет, плюс наименование Калинина, статус Кинери, Старый Буртек.
- Публиковать ли разделы Нослов и Дерюшева — лент у них не будет.
- Пункт «Праздники района» в меню шапки — добавить в админке (глобал «Шапка»), если нужен.
- Частота импорта раз в полчаса, до 33 вызовов за прогон — снизить, если для бюджета шлюза много.
