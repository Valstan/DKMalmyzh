# ДК Малмыж — Session Handoff

> Sticky-note для непрерывности сессий. Перезаписывается `/close_session`. История — `git log -- docs/SESSION_HANDOFF.md`.

**Status:** LIVE
**Updated:** 2026-08-31 (портал района: тесты, второй домен основным, импорт из ВК через шлюз Сарафана)
**Branch:** main

## Текущая нитка

Сайт перестал быть сайтом одного учреждения и стал **порталом культуры Малмыжского района**.

- **Основной домен — https://культура.вмалмыже.рф/** (punycode `xn--80atdujec4e.xn--80adkdyec4j.xn--p1ai`).
- Прежний `домкультуры.вмалмыже.рф` жив и отдаёт **301**: корень → `/dk/rckd`, остальное путь-в-путь.
- Сертификат LE выписан **на оба имени** одной линией `live/<новый>/`, до 2026-11-29.

Ждём **ключ шлюза SARAFAN** — без него импорт из ВК стоит выключенным. Заявка ушла: `mailbox/to-brain/2026-08-31-vk-gateway-key-request.md`.

⚠️ Инфра-детали прода (адрес хоста, порт, состав бокса, адрес шлюза Сарафана) в репозитории намеренно **не хранятся** — см. `AGENTS.md` §Recon-поверхность.

## Выполнено 2026-08-31 (вторая сессия дня)

Десять PR, #26–#35. Все гейты зелёные, прод проверен снаружи.

- **Тесты, которых не было вовсе (#26, #27).** vitest — 65 юнитов; playwright — 12 e2e на прод-сборке в браузере. Закрыты **все три остатка ревизии #104**: гейт открывает страницы, динамические маршруты проверяются, тестовый рантайм есть.
- **Контент-модель портала (#28).** Коллекция `institutions` (+ версии), связь `posts.institution`, вид записи `type` (новость/афиша). Афиша — НЕ отдельная коллекция: это тот же материал с датой, разведение удвоило бы маршруты, сид, миграции и импорт.
- **Маршруты (#29).** `/dk` — список ДК, `/dk/[slug]` — раздел учреждения со своей лентой и афишей. Главная — общая лента всех ДК с бейджем учреждения плюс блок ближайших событий. Название сайта → «Культура Малмыжского района».
- **Второй домен (#30, #34).** Сертификат на два имени (`--cert-name` по новому — новая линия, не `--expand`), 301 со старого, `vars.NEXT_PUBLIC_SERVER_URL` переключён, sitemap и каноникалы на новом домене.
- **Импорт из ВК (#31, #35).** Схема (`vkUid` уникальный, `source`, галерея), разбор ссылок и текста, конвертация в lexical, systemd-таймер. Ходит **только через шлюз SARAFAN**.
- **Две дыры, найденные по ходу (#32, #33).** См. ниже — обе были живыми.

## ⚠️ Прод сегодня ломался. Как и почему

Гейт миграций проверял только `HEAD~1..HEAD`. PR #28 с миграцией он остановил — верно. Следующий PR #29 миграции в диффе не имел, гейт его пропустил, и **код без схемы уехал на прод**: `/api/posts` и `/api/institutions` отдавали 500.

Снаружи это было **почти не видно**: публичные страницы возвращали 200, потому что views мягко деградируют к пустому списку. Ровно тот класс, которого проект и опасается — проверка выглядела пройденной.

Починено:
- **#33** — гейт сверяется с таблицей `payload_migrations` **на проде**, а не с прошлым коммитом. Нечитаемый реестр останавливает деплой: «не смогли проверить» ≠ «всё в порядке». Проверен живым прогоном на деплое #34.
- **#32** — `apply-migration.yml` не работал **вовсе**: читал env прода без `sudo` → `DATABASE_URI` пустой → psql шёл на локальный сокет. Preflight это прятал (`echo "$(psql …)"` — ошибка тонет в подстановке).

Прод восстановлен: обе миграции накачены, деплой прогнан, оба API отдают 200.

## Грабли и находки этой сессии

- **Гейт, проверяющий диффы, а не состояние, закрывает только первый шаг.** Миграция «уезжает» следующим коммитом. Источник правды — состояние прода.
- **`echo "$(команда)"` глушит ошибку** — команда подстановки падает, шаг рапортует «ок». Так `apply-migration` два месяца выглядел рабочим.
- **Heredoc в Bash-инструменте съедает `\` + перенос строки.** Многострочные команды в YAML собирались в одну; синтаксис оставался валидным, но читать невозможно. Проверять `bash -n` по извлечённому телу шага (правило D-046 — текст файлом — тут не помогает, оно про другое).
- **`payload migrate:create` спрашивает про rename интерактивно.** Пайп из `printf` закрывает stdin раньше второго вопроса; работает `{ sleep 8; printf '\033[B\r'; sleep 3; … }`.
- **Payload требует поле с `defaultValue`, если оно `required`:** сид падал типом, пока не проставили `source` явно.
- **`.sql` надо ИЗВЛЕКАТЬ из `up()`, а не переписывать.** Скрипт извлечения + проверка «`migrate:create` отвечает No schema changes detected» — два раза подряд дали полное зеркало.
- **Локальный PostgreSQL 17 на машине разработчика есть** (`postgres/postgres`, psql в `C:\Program Files\PostgreSQL\17\bin`). Весь гейт гоняется локально за пару минут вместо круга через CI.
- **Прежние грабли живы:** бокс делят несколько сайтов (маркер контента, теперь «Малмыж», а не «РЦКД»); TLS терминирует сам nginx; nginx 1.24 → `http2` параметром `listen`; кириллица в `grep -i` не матчится.

## Дыры в гейтах — что осталось из ревизии #104

**Все три закрыты.** Тесты есть (65 юнитов + 12 e2e), гейт открывает страницы в браузере, динамические маршруты (`/news/[slug]`, `/pages/[slug]`, `/dk/[slug]`) проверяются на засеянных документах.

Добавились новые проверки: импорт из ВК на подставных записях (идемпотентность), 301 со старого домена в smoke деплоя, сверка миграций с реестром прода.

## Как устроен импорт из ВК (не сломать при правках)

**В ВК ходим только через шлюз SARAFAN** — `AGENTS.md` §ВКонтакте. Свой VK-токен не заводится никогда, fallback на прямой `api.vk.com` не пишется: VK привязывает токен к IP выпуска (раздача технически не работает), а fallback обесценивает отзыв ключа — у соседа именно так «отзыв ключа сегодня ничего не отзывает».

- Запуск: таймер systemd → `POST /internal/vk-sync` у **уже работающего приложения**. Отдельного процесса нет: на прод едет standalone-бандл без payload CLI, а фото пишутся в `MEDIA_DIR`, который есть только на боксе.
- Маршрут закрыт дважды: секрет в заголовке (сравнение постоянного времени) + `deny` в nginx. Незаданный секрет = выключено, а не «пускать всех».
- Дедупликация — уникальный `vkUid` формата `owner_post`. Не составной индекс: его пришлось бы дописывать в `.sql` руками, и следующий `migrate:create` предложил бы его снести.
- Фото — в поля `cover`/`gallery`, НЕ upload-узлами в richText: наш `RichText` сложные узлы не рисует, картинки исчезли бы молча (мимо G230).
- Вид записи импорт **не угадывает**: всё приезжает новостью, афишу ставит редактор. По умолчанию — **черновики** (`VK_SYNC_PUBLISH=1` включает публикацию).
- Пауза 2,1 с между вызовами: квота ключа 30/мин. `429` с `Retry-After` — штатное событие с одним повтором.

## Следующий шаг

1. **Ключ шлюза.** Дождаться `GATEWAY_KEY_DKMALMYZH`, положить в `/etc/dkmalmyzh/dkmalmyzh.env` вместе с `SARAFAN_GATEWAY_URL` и `VK_SYNC_SECRET`. Деплой сам включит таймер, когда обе переменные появятся.
2. **Каталог ДК.** `pnpm payload run scripts/seed-institutions.ts` заведёт 31 карточку (27 со ссылкой на ВК) **черновиками**. Пройти глазами и опубликовать: у шести учреждений по две страницы ВК, у трёх это личный профиль, у четырёх сообщества нет вовсе.
3. **Первый импорт** — с `VK_SYNC_COUNT` побольше, разово, и посмотреть, что приехало, до публикации.

## Открытые вопросы владельцу

- **Список групп ВК от владельца** — обещан, ещё не пришёл. Наш собранный список в `web/scripts/institutions-data.ts`; сверить.
- Стоит ли добавить в привязку ключа личную страницу РЦКД (`+234960216`) — она активнее группы `dk_malmyzh`.
- Частота импорта: сейчас раз в полчаса. Если для общего бюджета шлюза много — снизим.
- **Не наша находка, но срочная:** в публичном `Valstan/postopus`, `src/config.py` — живые VK/Telegram-токены, пароли VK/Instagram/TikTok и строка подключения MongoDB открытым текстом. Отзыв за владельцем. Сообщено Мозгу тем же письмом.
