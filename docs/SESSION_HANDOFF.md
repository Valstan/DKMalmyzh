# ДК Малмыж — Session Handoff

> Sticky-note для непрерывности сессий. Перезаписывается `/close_session`. История — `git log -- docs/SESSION_HANDOFF.md`.

**Status:** IN PROGRESS
**Updated:** 2026-08-02 (праздничный визуальный редизайн подготовлен в отдельной ветке)
**Branch:** codex/festive-visual-redesign

## Текущая нитка

Активная нитка: праздничный визуальный редизайн публичного сайта. Сайт живёт на проде по адресу **https://домкультуры.вмалмыже.рф/** (Бокс 1, :3005, свой LE-сертификат); контент всё ещё ждёт наполнения владельцем из ВК.

## В работе (2026-08-02)

- Полностью переработан публичный интерфейс: праздничная палитра, витиеватые рамки, разные цветные секции, hero, тематические карточки Сабантуя / Казанской / концертов / театра, яркие news и footer.
- По референсу `D:\Скачано с интернета\мэри.jpg` через ImageGen создана отдельная оригинальная акварельная эмблема летящей женщины с зонтом; подготовлены прозрачный PNG, favicon и apple-touch icon.
- Учтены responsive-вёрстка и `prefers-reduced-motion`.
- Проверки: `pnpm typecheck` — зелёный; `pnpm lint` — зелёный с четырьмя прежними warning в initial migration. Локальный визуальный QA заблокирован: `next dev` остаётся на `Starting…` и не отвечает по HTTP (окружение Payload/PostgreSQL).

## Что сделано (2026-08-02)

- **ADR-0011:** `AGENTS.md` — канон для любого агента, `CLAUDE.md`/`GEMINI.md` — тонкие адаптеры; указатели переведены до сжатия (PR #4).
- **Смена домена.** `дкмалмыж.рф` просрочен у регистратора (парковка Ru-Center, продление до 24.08.2026) — решение владельца: не продлевать. Сайт переехал на **домкультуры.вмалмыже.рф** (punycode `xn--d1amdcjpngc5fh.xn--80adkdyec4j.xn--p1ai`), DNS уже смотрел на бокс. Обновлены код, `.env.example`, `vars.NEXT_PUBLIC_SERVER_URL` (PR #5).
- **Публикация наружу.** Свой vhost из репо (`deploy/nginx-dkmalmyzh-tls.conf`, бутстрап-версия `nginx-dkmalmyzh.conf`), LE-сертификат через `setup-tls.yml` (certbot --webroot), старый vhost мёртвого домена отключён с бэкапом (`~/dkmalmyzh/retired/`). Проверено снаружи: https 200, http→https 301, `/news`, `/admin` 200 (PR #7, #8, #9).
- **Гейты переехали в CI** на GitHub Actions (`ci.yml`, эфемерный Postgres) — локальный Postgres для `build` больше не нужен.
- **Подпись автора в подвале** со ссылкой на портфолио — mandate Мозга от 01.08 выполнен.

## Грабли этой сессии (не наступать снова)

- **Бокс делят несколько сайтов**; у гоньба.рф `listen 443 ssl default_server`. Проверка «curl -H Host → 200» ничего не доказывает — нужен контент-маркер («РЦКД»). Диагностика: `probe-nginx.yml` (read-only, `nginx -T`).
- **TLS терминирует сам nginx на боксе**, а не прокси Джино (это схема бокса Сабантуя — перенос её сюда и был первой ошибкой).
- ACME-webroot — в `/var/www`, не в домашнем каталоге (nginx-воркер туда не ходит, `try_files … =404` маскирует отказ под 404).
- На боксе **nginx 1.24**: `http2` — параметр `listen`, не отдельная директива.

## Следующий шаг

Проверить визуал на CI/preview, затем смержить PR и дождаться автодеплоя. После этого — наполнить сайт контентом владельца. Отложенное: письмо Мозгу по гейт-ревизии #104 (mandate от 28.07) и снапшот-грабля миграций (recommend от 26.07).

## Открытые вопросы владельцу

- **До 24.08.2026** старый `дкмалмыж.рф` ещё можно выкупить у Ru-Center; после этой даты он освобождается для всех. Текущее решение — не продлевать; если оно изменится, окно закрывается этой датой.
- Наполнение сайта контентом (из ВК) — сайт стоит пустым каркасом на живом домене.
