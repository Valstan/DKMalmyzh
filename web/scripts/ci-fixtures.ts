// Единственный источник правды о том, что кладёт сид и что открывает e2e.
// Раздельные литералы в сиде и в спеках разъезжаются молча — тест начинает
// проверять не тот документ или падает на «404», выглядящем как поломка роутера.
//
// Slug'и заданы явно, а не выведены из заголовка: автослуг у нас кириллический
// (домен IDN), и URL в спеках пришлось бы percent-кодировать — лишний источник
// расхождений в гейте, который должен ловить ошибки рендера, а не кодировки.
export const CI_PAGE_SLUG = 'ci-page'
export const CI_PAGE_TITLE = 'CI: тестовая страница'

export const CI_POST_SLUG = 'ci-post'
export const CI_POST_TITLE = 'CI: тестовая новость'
export const CI_POST_TITLE_UPDATED = 'CI: тестовая новость (обновлена)'

export const CI_INSTITUTION_SLUG = 'ci-dk'
export const CI_INSTITUTION_TITLE = 'CI: тестовый дом культуры'
export const CI_INSTITUTION_TITLE_UPDATED = 'CI: тестовый дом культуры (обновлён)'

// Афиша с датой в будущем: главная показывает только предстоящие события
// (date >= сейчас), и без такого документа эта ветка отбора не исполняется вовсе.
// Дата считается от текущего момента, а не задана литералом: захардкоженный год
// однажды окажется в прошлом, и гейт тихо перестанет проверять фильтр.
export const CI_EVENT_SLUG = 'ci-event'
export const CI_EVENT_TITLE = 'CI: будущая афиша'
export const ciEventDate = (): string =>
  new Date(Date.now() + 30 * 24 * 60 * 60 * 1000).toISOString()

// Черновики. Гейт обязан проверять не только что опубликованное ВИДНО, но и что
// неопубликованное НЕ видно: на проде сейчас сотни черновиков, и утечка любого из
// них в публичную ленту — это чужой материал, показанный без решения редакции.
// Без такого документа в БД ни один тест не отличает «фильтр работает» от
// «фильтровать было нечего».
export const CI_DRAFT_POST_SLUG = 'ci-draft-post'
export const CI_DRAFT_POST_TITLE = 'CI: черновик новости (не должен быть виден)'

export const CI_DRAFT_INSTITUTION_SLUG = 'ci-draft-dk'
export const CI_DRAFT_INSTITUTION_TITLE = 'CI: черновик дома культуры (не должен быть виден)'
